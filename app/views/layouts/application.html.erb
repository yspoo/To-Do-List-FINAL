<!DOCTYPE html>
<html>
  <head>
    <title>ToDoList</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://use.fontawesome.com/10f489fef6.js"></script>
  </head>

  <body>

    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to "TaskList", root_path, class: "navbar-brand" %>
          <!-- class: "navbar-brand" is a class we added. it has to be added after the path and has to be "class:" and not "class=". All the classes are Bootstrap classes. -->
        </div>
        <ul class="nav navbar-nav">
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
          <% if current_user.present? %>
            <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
          <% else %>  
            <li><%= link_to "Log In",new_user_session_path, method: :get %></li>

          <% end %>
        </ul>
        <% if user_signed_in? %>
          <%= link_to "New Task", new_task_path, class: "navbar-text navbar-link navbar-right"%>
        <% end %>
      </div>  
    </nav>    

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>



    <div class="container">
      <%= yield %>
    </div>

    



  </body>
</html>
